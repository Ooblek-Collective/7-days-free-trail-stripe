# node_modules
node_modules

# local secrets
.env

# user database (password hashes, Stripe customer/subscription IDs)
data.sqlite
data.sqlite-wal
data.sqlite-shm

# generated by `vite build` (see client/) - public/index.html and
# public/dashboard.html are build output now, not hand-written source
public